spring:
  application:
    name: user-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    user: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true


server:
  port: 8082


